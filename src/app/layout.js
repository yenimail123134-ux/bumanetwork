export const metadata = {
  title: 'Buma Network | oyna.bumamc.com',
}

export default function RootLayout({ children }) {
  return (
    <html lang="tr">
      <body style={{ margin: 0, background: '#020202' }}>{children}</body>
    </html>
  )
}
